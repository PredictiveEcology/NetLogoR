% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agentMatrix-Class-methods.R
\name{coerce-agentMatrix}
\alias{coerce-agentMatrix}
\alias{coerce,matrix,agentMatrix-method}
\alias{coerce,data.frame,agentMatrix-method}
\alias{coerce,agentMatrix,data.frame-method}
\title{Coerce to and from an \code{agentMatrix}}
\usage{
\S4method{coerce}{matrix,agentMatrix}(from, to = "agentMatrix", strict = TRUE)

\S4method{coerce}{data.frame,agentMatrix}(from, to = "agentMatrix", strict = TRUE)

\S4method{coerce}{agentMatrix,data.frame}(from, to = "data.frame", strict = TRUE)
}
\arguments{
\item{from}{The \code{matrix}, \code{data.frame}, or \code{agentMatrix} to coerce.}

\item{to}{Character string naming the class to coerce \code{from} to.}

\item{strict}{Logical, passed by \code{\link[methods:as]{methods::as()}}; unused by these methods.}
}
\value{
An \code{agentMatrix} or a \code{data.frame}, depending on the class coerced to.
}
\description{
Methods for \code{\link[methods:as]{methods::as()}} to convert a \code{matrix} or a \code{data.frame} into an
\code{agentMatrix}, and to convert an \code{agentMatrix} back into a \code{data.frame}.
When converting to an \code{agentMatrix}, the first two columns of \code{from} are
taken to be the coordinates, and the remaining columns become the agents'
variables. When converting back, variables stored as factors are restored
to their original character values.
}
\examples{
m <- cbind(xcor = c(1, 2, 5), ycor = c(3, 4, 6), nums = 5:7)
am <- as(m, "agentMatrix")
as(am, "data.frame")

}
\seealso{
\code{\link[=agentMatrix]{agentMatrix()}}
}
